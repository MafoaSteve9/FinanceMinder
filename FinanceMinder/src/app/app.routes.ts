import { Routes } from '@angular/router';
import { ConnexionFormComponent } from './connexion-form/connexion-form.component';

export const routes: Routes = [
    { path: 'form', component: ConnexionFormComponent}
];
